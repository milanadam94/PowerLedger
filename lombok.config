# Add annotation that tells JaCoCo which code is autogenerated.
# Otherwise, the unit test coverage report will not be correct
# because it will treat Lombok generated code as not-covered.
lombok.addLombokGeneratedAnnotation = true